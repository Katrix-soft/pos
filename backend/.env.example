# Puerto del backend (interno Docker, no exponer al host)
PORT=5000

# Entorno
NODE_ENV=production

# JWT — IMPORTANTE: cambiar por un string aleatorio largo en producción
JWT_SECRET=katrix_secret_key_change_me

# Ruta de la base de datos SQLite (en Docker: /data/katrix.db)
DB_PATH=/data/katrix.db
